# ----------------------------------------------------------------------------
# Python package requirements – fixed build errors for PyG extensions
# ----------------------------------------------------------------------------
# We add a “find-links” directive that points pip to the official PyG wheel
# repository hosting the pre-compiled CUDA 11.8 binaries that match
#   * Python 3.11
#   * torch 2.2.1 (pt22) 
# This prevents pip from attempting an in-tree source build (which fails
# because a full C/C++ tool-chain and the torch headers are missing in the
# isolated build environment).
# ----------------------------------------------------------------------------
--find-links https://data.pyg.org/whl/torch-2.2.1+cu118.html

# Core deep-learning stack ----------------------------------------------------
torch==2.2.1              # cu118 wheels available for cp311
pyg-lib==0.4.0            # low-level CUDA kernels used by PyG
torch_scatter==2.1.2      # message-passing primitives
torch_sparse==0.6.18      # sparse-matrix ops
torch_cluster==1.6.3      # graph clustering utils
torch_spline_conv==1.2.2  # spline-based convolutions
torch_geometric==2.5.0    # high-level GNN library

# Scientific Python stack -----------------------------------------------------
numpy
scipy
pandas
scikit-learn
matplotlib
seaborn
tqdm
pyyaml
ogb
