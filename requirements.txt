# ----------------------------------------------------------------------------
# Python package requirements – revised
# ----------------------------------------------------------------------------
# Use the official wheels published on PyPI so that the solver can pick
# the correct build (CPU-only or CUDA) for the running environment.
# We therefore remove the explicit "+cpu" build tags that previously broke
# the installation on Python 3.12 / CUDA-enabled runners.
# ----------------------------------------------------------------------------

torch==2.3.0                 # automatically pulls the proper CUDA / CPU wheel
torch_geometric==2.5.0        # compatible with PyTorch ≥2.2
# Explicit pin so that the dependency resolver does not attempt a source build
# (a pre-built wheel for the active Python / PyTorch combination is available).
torch_scatter==2.1.2

# Scientific Python stack
numpy
scipy
pandas
scikit-learn
matplotlib
seaborn
tqdm
pyyaml
ogb
